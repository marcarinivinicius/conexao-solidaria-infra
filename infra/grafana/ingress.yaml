apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grafana-ingress
  namespace: conexao-solidaria
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: grafana.conexao-solidaria.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: grafana
                port:
                  number: 3000
